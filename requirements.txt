# Data / preprocessing
pypdf>=4.0.0
langchain-text-splitters>=0.2.0

# RAG (retrieval)
sentence-transformers>=2.2.0
numpy>=1.22.0
torch>=2.0.0

# HF Inference API for reader LLM (course: open HF models only; set HF_TOKEN if needed)
huggingface-hub>=0.23.0
httpx>=0.27.0
